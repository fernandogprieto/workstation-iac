# kind-cluster.yaml - multi-node cluster for CKA practice
# The CKA exam uses multi-node clusters, so practice on one.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
